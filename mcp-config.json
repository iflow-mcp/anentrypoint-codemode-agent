{
  "servers": {
    "filesystem": {
      "command": "node",
      "args": ["index.js"],
      "description": "File system operations (Read, Write, Edit, Glob, Grep, Bash, etc.)"
    },
    "codeMode": {
      "command": "node",
      "args": ["code-mode.js"],
      "description": "Code execution and development tools"
    }
  }
}